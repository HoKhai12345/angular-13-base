export const API_CONFIG = {
    baseUrl: '',
    excludedUrls: [
      '/api/login',
      '/api/register',
      '/api/forgot-password',
      '/api/verify-token'
    ],
    timeout: 5000, // Thời gian timeout cho các request
  };